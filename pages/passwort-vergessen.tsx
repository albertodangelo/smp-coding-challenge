import React from 'react';
import { Typography } from '@material-ui/core';

export default function PasswortReminder() {
  return (
    <div>
      <Typography style={{ marginTop: '40vh', textAlign: 'center' }}>
        Diese Funktion wurde nicht implementiert.
      </Typography>
    </div>
  );
}
